aliases:
  - 'handlebars_layout'
watch:
  files:
    - '<%= dev %>/index.html'
  tasks:
    - 'view'
handlebars_layout:
  options:
    basePath: '<%= dev %>'
    partials:
      - '<%= dev %>/layouts/**/*.html'
      - '<%= dev %>/modules/**/*.html'
    data:
      - '<%= dev %>/data/**/*.yaml'
  files:
    '<%= dist %>/' : '<%= dev %>/*.html'
